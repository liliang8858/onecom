[project]
name = "enterprise-agent"
version = "0.1.0"
description = "Enterprise-grade AI Agent learning and implementation project."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

